#!/usr/bin/env python3
"""
Cronus Agent CLI launcher.

This wrapper should behave like the installed `cronus` command, including
subcommands such as `gateway`, `cron`, and `doctor`.
"""

if __name__ == "__main__":
    from cronus_cli.main import main
    main()
